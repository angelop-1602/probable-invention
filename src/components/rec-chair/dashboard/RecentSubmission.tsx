import * as React from "react";

export default function RecentSubmission() {
    return (
        <section className="bg-background p-6 rounded-lg shadow border">
            <h2 className="text-xl font-semibold mb-4">Recent Submissions</h2>
            <p className="text-muted-foreground italic">No recent submissions found.</p>
        </section>
    );
}
